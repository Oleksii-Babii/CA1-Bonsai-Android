<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BonsaiApp"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SpeciesListActivity"
            android:exported="false"
            android:label="@string/label_species"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".SpeciesDetailActivity"
            android:exported="false"
            android:label="@string/label_species_detail"
            android:parentActivityName=".SpeciesListActivity" />

        <activity
            android:name=".TreeListActivity"
            android:exported="false"
            android:label="@string/label_trees"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".TreeDetailActivity"
            android:exported="false"
            android:label="@string/label_tree_detail"
            android:parentActivityName=".TreeListActivity" />

        <activity
            android:name=".AddEditTreeActivity"
            android:exported="false"
            android:label="@string/label_add_tree"
            android:parentActivityName=".TreeListActivity" />

    </application>

</manifest>
